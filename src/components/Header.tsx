import React from "react";

interface Props {}

const Header = (props: Props) => {
  return (
    <header>
      <div className="header__container">
        <h1>Split Shopper</h1>
      </div>
    </header>
  );
};

export default Header;
